name: CI learning

on:
    push:
        branches: ['main']

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout version
              uses: actions/checkout@v4
            
            - name: Print meassage
              run: echo "Hello,CICD"

            - name: Run a script
              run: |
                echo "Running script..."
                ls -la

            - name: Check the File
              run: cat hello.txt

            - name: Check if running or not
              run: |
                echo "file not found"
                ls -la


